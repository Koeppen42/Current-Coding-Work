import React from "react";
import ReactDOM from "react-dom";

const fName = "Ted";
const lName = "Scott";
const numb = 42;

ReactDOM.render(
  <div>
    <h1>Hello {fName + " " + lName}!</h1>
    <p>Your lucky number is {numb}</p>
  </div>,
  document.getElementById("root")
);
